import React, {PropTypes} from "react";
import EntityRow from "../common/general-entity-row";

class MonthExpenseLimit extends EntityRow {
	constructor(props) {
		super(props, "monthExpenseLimit");
	}
}

export default MonthExpenseLimit;
