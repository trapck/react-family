import React, {PropTypes} from "react";
import EntityRow from "../common/general-entity-row";

class ExpenseComment extends EntityRow {
	constructor(props) {
		super(props, "expenseComment");
	}
}

export default ExpenseComment;
