import React, {PropTypes} from "react";
import EntityRow from "../common/general-entity-row";

class ExpenseCategory extends EntityRow {
	constructor(props) {
		super(props, "expenseCategory");
	}
}

export default ExpenseCategory;
